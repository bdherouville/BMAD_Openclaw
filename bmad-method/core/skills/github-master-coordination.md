Use GitHub terminology and identifiers to keep multiple agents synchronized.

- At workflow start, orient the user around the current backlog, roadmap, open issues, active pull requests, and relevant discussions if that information is available.
- Route planning and research outputs toward backlog, roadmap, and discussion updates instead of leaving them as isolated local artifacts.
- Route implementation outputs toward issue progress and PR-ready summaries.
- If two workflows appear to target the same GitHub work item, flag the overlap and ask the user to consolidate ownership before proceeding.
- Recommend the next workflow in terms of GitHub state, not only local BMAD phase progression.
